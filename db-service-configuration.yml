server:
  port: 8030
  ssl:
      key-password: password
      key-store-password: password
      key-store: src/main/resources/keystore.jks
      kay-store-type: pkcs12
spring:
  application:
    name: db-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/quotes
    username: root
    password: root